-- Show all databases
SHOW DATABASES;

-- Use your database
USE VITBHOPAL;

-- Show tables from VITBHOPAL
SHOW TABLES FROM VITBHOPAL;

-- Display all records from CSE
SELECT * FROM CSE;

-- Describe structure of CSE table
DESC CSE;

-- Add new columns
ALTER TABLE CSE ADD(
    sphn BIGINT,
    sphn2 BIGINT
);

-- Insert new record
INSERT INTO CSE(id, name, marks)
VALUES (106, 'Semi', 95);

-- Drop column sphn2
ALTER TABLE CSE DROP COLUMN sphn2;

-- Add country column with default value
ALTER TABLE CSE ADD(
    sCountry VARCHAR(30) DEFAULT 'INDIA'
);

-- Rename column
ALTER TABLE CSE
RENAME COLUMN sCountry TO SLocation;





-- Display all records from CSE
SELECT * FROM CSE;

-- Drop CSE table
DROP TABLE CSE;

-- Describe CSE table
DESC CSE;

-- Show all tables from your database
SHOW TABLES FROM VITBHOPAL;

-- Delete all records from EEE table
DELETE FROM EEE;

-- Display all records from EEE
SELECT * FROM EEE;

-- Rename EEE table to ECE
RENAME TABLE EEE TO ECE;

-- Create Worker table
CREATE TABLE Worker (
    WORKER_ID INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    FIRST_NAME CHAR(25),
    LAST_NAME CHAR(25),
    SALARY INT,
    JOINING_DATE DATETIME,
    DEPARTMENT CHAR(25)
);

-- Insert records into Worker table
INSERT INTO Worker
(WORKER_ID, FIRST_NAME, LAST_NAME, SALARY, JOINING_DATE, DEPARTMENT) VALUES
(001, 'Monika', 'Arora', 100000, '2014-02-20 09:00:00', 'HR'),
(002, 'Niharika', 'Verma', 80000, '2014-06-11 09:00:00', 'Admin'),
(003, 'Vishal', 'Singhal', 300000, '2014-02-20 09:00:00', 'HR'),
(004, 'Amitabh', 'Singh', 500000, '2014-02-20 09:00:00', 'Admin'),
(005, 'Vivek', 'Bhati', 500000, '2014-06-11 09:00:00', 'Admin'),
(006, 'Vipul', 'Diwan', 200000, '2014-06-11 09:00:00', 'Account'),
(007, 'Satish', 'Kumar', 75000, '2014-01-20 09:00:00', 'Account'),
(008, 'Geetika', 'Chauhan', 90000, '2014-04-11 09:00:00', 'Admin');

-- Display Worker table
SELECT * FROM Worker;

-- Update worker name
UPDATE Worker
SET FIRST_NAME='Jayanth',
    LAST_NAME='V'
WHERE WORKER_ID=1;

-- Increase salary of all workers
UPDATE Worker
SET SALARY = SALARY + 5000;

-- Delete worker with ID 8
DELETE FROM Worker
WHERE WORKER_ID=8;



-- Start Transaction
START TRANSACTION;

-- Create Table
CREATE TABLE emp (
    id INT
);

-- Insert first record
INSERT INTO emp VALUES (101);

-- Display table
SELECT * FROM emp;

-- Create Savepoint
SAVEPOINT time15;

-- Insert second record
INSERT INTO emp VALUES (102);

-- Create another Savepoint
SAVEPOINT time16;

-- Display records
SELECT * FROM emp;

-- Insert third record
INSERT INTO emp VALUES (103);

-- Create Savepoint
SAVEPOINT time17;

-- Rollback to time17
ROLLBACK TO time17;

-- Insert fourth record
INSERT INTO emp VALUES (104);

-- Create Savepoint
SAVEPOINT time18;

-- Rollback again to time17
ROLLBACK TO time17;

-- Display final records
SELECT * FROM emp;



-- Show Databases
SHOW DATABASES;

-- Use Database
USE VITBHOPAL;

-- Show Tables
SHOW TABLES FROM VITBHOPAL;

-- Display CSE Table
SELECT * FROM CSE;

-- Equal To Operator (=)
SELECT * FROM CSE
WHERE marks = 95;

-- Greater Than Operator (>)
SELECT * FROM CSE
WHERE marks > 85;

-- Less Than Operator (<)
SELECT * FROM CSE
WHERE marks < 90;

-- Greater Than or Equal To Operator (>=)
SELECT * FROM CSE
WHERE marks >= 88;

-- Less Than or Equal To Operator (<=)
SELECT * FROM CSE
WHERE marks <= 80;

-- Not Equal To Operator (!=)
SELECT * FROM CSE
WHERE marks != 92;